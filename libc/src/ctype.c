#include <lf/ctype.h>

/* The character API is header-only for now. This translation unit reserves a
 * stable home for future locale-aware implementations. */
int lf_ctype_anchor(void) { return 0; }
